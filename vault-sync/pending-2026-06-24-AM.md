# Pending vault updates — 2026-06-24 AM

Staged by the morning vault-sync check. Covers the 4 commits since the last staging note (`pending-2026-06-24-PM.md`, written before these landed). Pull what's relevant into Obsidian, then this file can be archived/deleted.

- **Svitna (separate repo) gets its "Jon Svitna" coach identity + goal-tailored accountability nudges.** Logged in `decisions/log.md` (2026-06-23 entry, commit `77a78be`). Ground-truthed the app first (it does build, backend is live on Railway, routes are real — not the "broken wiring" Sean initially suspected); the actual blocker to an App Store soft test was no differentiated voice. Rewrote the AI coach's system prompt to speak as Jon Svitna (tough-love, direct), built a same-day local-notification nudge engine keyed off existing workout/streak data (no new data sources invented), and did a copy pass across onboarding/coach/dashboard/notifications. Also walked back an earlier "rotate credentials" call on `Secrets.xcconfig` — confirmed not a real secret (Auth0 native/PKCE client ID). Next: simulator walkthrough to confirm voice + nudge trigger before wider rollout. This is product-direction for Svitna, not one of the three tracked lines (Sentinel CMMC/SEO agent/website dev) — note under `wiki/decisions/` if Svitna has a vault home yet.

- **Hosted Claude Code CLI on a DigitalOcean droplet for remote/mobile access.** Logged in `decisions/log.md` and `connections.md` (commit `35761d8`). Droplet `vulnaguard-aios` (NYC1, IP `143.244.148.90`), hardened (non-root sudo user, key-only SSH, UFW), running Claude Code in a persistent tmux session reachable via Termius from phone. Rationale: sidesteps a work-network block on claude.ai by routing over owned SSH instead. Ruled out Slack-native integration and a custom Agent SDK+Slack bridge first. Caveat: droplet currently only has this public repo cloned — private repos (Sentinel-CMMC, AfterSwing, etc.) need `gh auth` or a deploy token before they're usable there.

- **Disabled two stale Exchange forwarding rules to a third party's personal Gmail.** Logged in `decisions/log.md` (commit `e737e21`). Two inbox rules on `seanmurrill@vulnaguard.com` forwarding to `jessicasayre28@gmail.com` were disabled (not deleted, easy to re-enable). Side-finding: `MailboxSettings.ReadWrite` — previously flagged as "pending" and cited as the reason mail/lead routing moved to Slack on 2026-06-20 — turned out to already be granted; the rules had been live the whole time. `connections.md` and `references/microsoft365-api.md` updated to reflect the granted permission and the new mailbox rule redirecting mail to `#leads-inbox` in Slack. Also added a `create-rule` CLI command (`scripts/microsoft365_api.py`).

No leads/inbox.md, leads/website-design-inbox.md, or context/ changes in this window — nothing new to triage on the lead or priorities side.
