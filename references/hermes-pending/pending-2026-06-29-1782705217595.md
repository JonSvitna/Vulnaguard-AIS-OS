# Hermes pending — 2026-06-29

Auto-staged by the hermes-cron Railway service. Review and merge worthwhile entries into `references/hermes-opportunities.md`, then delete this file.

## SeanBuilds

### [unused] 2026-06-29 — main-thread-freeze-vision-pose-analyzer
**Domain:** SeanBuilds
**Pillar guess:** Lesson from a mistake — what went wrong, what changed after
**Source:** commit a92d79a in AfterSwing
**Hook:** A one-line Task.detached fix eliminated a full UI freeze during swing analysis — 60 Vision frame decodes across a 12-second clip were silently running on the main thread, making the app look permanently hung.
**Talking points:**
1. @MainActor inheritance is silent and invisible — VisionPoseAnalyzer had no isolation of its own, so calling it directly pulled all 60+ image decode + body-pose detection calls onto the main thread with no warning
2. The symptom looked exactly like a permanent hang rather than slowness, making it hard to diagnose without knowing Swift's actor model deeply
3. Task.detached is the escape hatch: it severs the calling actor's context entirely, forcing work onto a background executor — one wrapper, zero architecture changes
**Status:** unused
