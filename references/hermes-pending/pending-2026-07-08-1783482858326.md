# Hermes pending — 2026-07-08

Auto-staged by the hermes-cron Railway service. Review and merge worthwhile entries into `references/hermes-opportunities.md`, then delete this file.

## Vulnaguard

### [unused] 2026-07-08 — four-touch-sequence-scheduling-bug
**Domain:** Vulnaguard
**Pillar guess:** Behind the build — process, tools, decisions
**Source:** commit a64231c in vulnaguard-seo-agent
**Hook:** Expanding from 3 to 4 email touches exposed a hardcoded scheduling bug that would have left every 4th email with scheduled_at = NULL — silently never sending.
**Talking points:**
1. The approval endpoint had touch_number 2 and 3 hardcoded by column — any sequence longer than 3 touches would generate the email but never schedule it, with no error
2. Fix generalizes scheduling to query actual touch numbers from the DB and walk the delays array dynamically, so 3-touch and 4-touch sequences both work without code changes
3. commercial_security now gets intro → follow-up → different-angle follow-up → breakup, while cmmc and website_dev stay at 3 touches — business-line-specific cadences enforced at the validation layer
**Screenshot:** screenshots/hermes/four-touch-sequence-scheduling-bug.png
**Status:** unused

### [unused] 2026-07-08 — llm-ignores-hard-rules-deterministic-fix
**Domain:** Vulnaguard
**Pillar guess:** Lesson from a mistake — what went wrong, what changed after
**Source:** commit 6523b75 in vulnaguard-seo-agent
**Hook:** The AI outreach agent shipped a live LinkedIn message with an em dash despite a hard 'no em dashes' rule — so Sean stopped trusting the prompt and added a deterministic character substitution that makes it structurally impossible.
**Talking points:**
1. Prompt rules alone aren't enforcement — the model followed the rule in training but still violated it in production on a live send
2. Two-layer fix: added the rule explicitly to the self-check prompt checklist AND wrote a post-processing function that replaces every em dash with a comma before the message leaves the system
3. The comment in the code is the philosophy: 'safe to substitute deterministically since it's a pure character swap, unlike banned-phrase rewrites which need judgment' — knowing which corrections require AI and which don't is a real engineering decision
**Screenshot:** screenshots/hermes/llm-ignores-hard-rules-deterministic-fix.png
**Status:** unused

### [unused] 2026-07-08 — llm-dodges-exact-string-ban-by-inflecting
**Domain:** Vulnaguard
**Pillar guess:** Lesson from a mistake — what went wrong, what changed after
**Source:** commit c41fdfa in vulnaguard-seo-agent
**Hook:** The model wrote 'circling back' to dodge a hard ban on 'circle back' — so the prompt now bans word roots and a regex backstop flags any inflected form before a draft gets approved.
**Talking points:**
1. LLMs will find the exact loophole you leave open: banning a literal string doesn't ban its conjugations, and a live test proved it — 'circling back' sailed right through.
2. The fix is two-layer: update the prompt to explicitly name inflected forms so the model self-catches, then add a regex backstop (root-word patterns like /circl\w* back/i) that fires a warning at runtime before human approval.
3. This is a broader pattern for prompt guardrails — exact-match lists are fragile; root-regex audits at the application layer are the safety net when the model is incentivized to produce fluent output over strict compliance.
**Screenshot:** screenshots/hermes/llm-dodges-exact-string-ban-by-inflecting.png
**Status:** unused

### [unused] 2026-07-08 — can-spam-footer-deterministic-backstop
**Domain:** Vulnaguard
**Pillar guess:** Lesson from a mistake — what went wrong, what changed after
**Source:** commit 23afb2a in vulnaguard-seo-agent
**Hook:** A live test showed the AI skipped the required CAN-SPAM opt-out footer on outreach emails — so Sean added a code-level backstop that appends it deterministically, making it impossible to send a commercial email without the legal opt-out line regardless of what the model outputs.
**Talking points:**
1. Prompts alone aren't compliance — the model ignored explicit footer instructions in production, proving that legal requirements can't be delegated to an LLM's best effort
2. The fix is a two-layer defense: a rewritten 'FINAL CHECKLIST' prompt that shows the footer verbatim in the JSON schema example, plus a post-processing function that appends it if the model still skips it
3. This is the builder instinct to never trust probabilistic output for deterministic requirements — anything with a legal or financial consequence gets a code-level guarantee, not just a prompt instruction
**Screenshot:** screenshots/hermes/can-spam-footer-deterministic-backstop.png
**Status:** unused
