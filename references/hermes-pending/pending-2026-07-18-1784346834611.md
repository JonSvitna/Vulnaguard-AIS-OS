# Hermes pending — 2026-07-18

Auto-staged by the hermes-cron Railway service. Review and merge worthwhile entries into `references/hermes-opportunities.md`, then delete this file.

## Vulnaguard

### [unused] 2026-07-18 — resend-status-false-alarm-fix
**Domain:** Vulnaguard
**Pillar guess:** Before/after — a number that changed
**Source:** commit d34ff33 in vulnaguard-seo-agent
**Hook:** The Settings page showed Resend as 'unset' even when emails were actually sending live on Railway — a false alarm that looked identical to a real outage, now fixed with a live server check.
**Talking points:**
1. localStorage only knows what the browser stored — it has zero visibility into server-side env vars, so the UI was lying about the real state
2. One fetch to /api/marketing/config replaces the false signal with a live ● LIVE ON SERVER / ● NOT SET ON SERVER indicator pulled straight from Railway
3. False alarms erode trust in your own dashboards — if the warning always looks wrong, you start ignoring it, and then miss the real outage
**Status:** unused
