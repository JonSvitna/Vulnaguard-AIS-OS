# Hermes pending — 2026-06-30

Auto-staged by the hermes-cron Railway service. Review and merge worthwhile entries into `references/hermes-opportunities.md`, then delete this file.

## SeanBuilds

### [unused] 2026-06-30 — afterswing-auth-bypass-stale-onboarding-flag
**Domain:** SeanBuilds
**Pillar guess:** Lesson from a mistake — what went wrong, what changed after
**Source:** commit 95de4f1 in AfterSwing
**Hook:** A stale onboarding flag let users reach AfterSwing's main app with zero auth token — one flag check added blocks the bypass entirely.
**Talking points:**
1. The gate only checked hasCompletedOnboarding, so any user with that flag set (from a previous install or cleared session) could land in the full app with no token — every authenticated API call would fail silently.
2. Fix was a one-line AND condition (authToken != nil && hasCompletedOnboarding), but finding it required tracing the exact path a returning user takes through RootView — easy to miss when you build onboarding and auth as separate flows.
3. Removed 'Skip for now' in the same commit: once the gate requires a real token, the skip button just dead-ends users anyway — deleting dead code that would have caused confusion later.
**Status:** unused
