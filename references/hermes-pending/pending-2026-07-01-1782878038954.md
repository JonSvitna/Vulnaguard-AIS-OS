# Hermes pending — 2026-07-01

Auto-staged by the hermes-cron Railway service. Review and merge worthwhile entries into `references/hermes-opportunities.md`, then delete this file.

## SeanBuilds

### [unused] 2026-07-01 — camera-rotation-order-bug-fix
**Domain:** SeanBuilds
**Pillar guess:** Lesson from a mistake — what went wrong, what changed after
**Source:** commit 661374e in AfterSwing
**Hook:** Reordering two AVFoundation calls — locking the camera format before setting portrait rotation instead of after — eliminated the silent reset to 0° landscape that was sending users into an endless recapture loop.
**Talking points:**
1. AVFoundation's isVideoRotationAngleSupported(90) can return false while the session configuration is still open, so calling it before commitConfiguration() was always producing sideways frames that Vision couldn't analyze.
2. Changing activeFormat after setting the rotation angle silently resets the angle to 0° (sensor-native landscape) — no error, no warning, just broken output — a class of bug that only shows up at runtime on a real device.
3. The fix also added a framing guide overlay whose dimensions are mathematically tied to VisionPoseAnalyzer's minBodyHeight and maxCenterOffsetX thresholds, so a golfer who fills the bracket will actually pass the post-capture validation check.
**Status:** unused
